# _redirects — G-will Chijioke (https://gwillchijioke.com)

# Media uploads — update <YOUR_R2_CDN_DOMAIN> when R2 is configured.
# Until then, keep this commented out or images load from WP origin directly.
# /wp-content/uploads/* https://<YOUR_R2_CDN_DOMAIN>/:splat 301

# WP REST proxy — not needed; Astro fetches WP at build time.
# /wp-json/* /api/:splat 301
